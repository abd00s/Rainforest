<h1>All Products!</h1>
<% @products.each do |p| %>
	<strong><%= link_to "#{p.name}", product_path(p) %><br></strong>
	<%= p.description %><br>
	<%= p.price_in_cents.to_f/100 %>
	<br>
	<br>
<% end %>
<%= link_to "Add New Product", new_product_path %> 
